# FIT2094 Assignment 2 Instructions

You are helping with FIT2094 Assignment 2 Oracle SQL and MongoDB.

Rules:
- Do not rewrite the whole assignment unless explicitly asked.
- Do not edit Task 1 schema unless the user specifically asks.
- Use Oracle SQL syntax suitable for the Monash FIT2094 unit.
- Do not use PL/SQL, WITH, views, triggers, procedures, SPOOL, ECHO, or disabling constraints.
- Use simple ALTER TABLE, CREATE TABLE, INSERT, UPDATE, DELETE, SELECT, DESC, COMMENT ON COLUMN, PK, FK, UNIQUE, NOT NULL, and CHECK constraints only.
- Preserve existing data in live database tasks.
- Use sequences only where required.
- Do not manually look up primary key values. Use subqueries where required.
- Use TO_DATE for dates.
- Keep comments short: one or two sentences per block.
- Never invent assignment requirements. Ask to check the brief if uncertain.
- Before editing, explain the planned change.
- After editing, summarise exactly what changed.